@media  (max-width: 414px) and (min-width: 200px){
    /* Remove body styles to focus on video and canvas */
    /* :root {
        --duration: 60; 
    } */
    .tools button,
    .video-buttons .video-button,
    .bottom-right-sidebar button,
    .header-icon {
        background-color: transparent; /* No background */
        border: none; /* No border */
        box-shadow: none; /* No shadow */
        padding: 0; /* Remove padding for a flat look */
        margin: 2px; /* Adjust margin as needed */
        outline: none; /* Remove outline on focus */
        cursor: pointer; /* Pointer cursor on hover */
    }
      html, body {
        margin: 0;
        padding: 0;
        overflow: hidden; /* Prevent overflow */
        touch-action: none; 
        background-color: #252c31;
    }


    #video-container {
        position: fixed; /* Fixed to cover the entire viewport */
        top: 0;
        left: 0;
        width: 100%;
        height: 100vh; /* Full viewport height */
        margin: 0;
        padding: 0;
        z-index: 1; /* Place behind floating elements */
    }


    video, #canvas ,#media-element{

        width: 100%;
        height: 100vh; /* Full viewport height */
        margin: 0;
        padding: 0;
        object-fit: cover; /* Ensure the video covers the entire area */
    }


    video ,#media-element{

        width: 100%;
        height: 100vh;
        position: absolute;
        top: 0;
        left: 0;
        z-index: 1;
        pointer-events: none;
        object-fit: cover;
        margin: 0;
        padding: 0;
    }

    #canvas {
        width: 100%;
        height: 100vh;
        position: absolute;
        top: 0;
        left: 0;
        z-index: 2;
        pointer-events: none;
        background-color: transparent;
        object-fit: cover;
        margin: 0;
        padding: 0;
        cursor: pointer; 
    }

    .tools {
        opacity: 0.8;
        position: absolute;
        top: 130px;
        left: 0;
        height: 60%;
        width: 45px; /* Adjusted Sidebar width */
        display: flex;
        flex-direction: column;
        background-color: transparent;
        z-index: 1000; /* Float above video and canvas */
        padding-top: 12px;
        border-radius: 35px;
        padding-left: 5px;
        margin-left: 8px;
        font-size: 0;
    }

    .tools button {
        margin: 2px;
        padding: 0px;
        width: 45px;
        margin-left: 125px;
        text-align: center;
        background-color: rgba(52, 58, 64, 0.9);
    }

    /* .tools button img {
        width: 20px; /* Reduced icon size */
        /* height: 20px; Reduced icon size */
        
    /* /* } 
     #freehand{
        background-color: #b03e2a;
     }  */

     .video-buttons {
        position: absolute;
        bottom: 20px; /* Position 20px from the bottom of the screen */
        left: 50%;
        transform: translateX(-50%); /* Center horizontally */
        z-index: 1000; /* Float above video and canvas */
        display: flex;
        justify-content: center;
        background-color: transparent; /* Slightly transparent background */
        padding: 5px;
        border-radius: 5px;
    }

    .video-buttons .video-button {
        margin: 0 13px;
        margin-bottom: 14px;
        background-color: transparent;
    }

    .video-buttons svg {
        fill: #ffffff; /* White color for icons */
    }

    .volume-button-container{
        display: none;
    }

    .video-buttons-container{
        display:none;
    }

    .button-with-menu{
        display:none;
    }
    .annotations {
        display: none;
    }
    .save-container{
        position: relative;
        background: transparent;
        background-color: transparent;
    }
    .save-btn {
        display: none;
        position: fixed; /* Changed to fixed to stay relative to viewport */
        top: 10px; /* Distance from the top of the viewport */
        right: -45px; /* Distance from the right edge of the viewport */
        background: transparent;
        background-color: transparent;
        border: none;
        cursor: pointer;
        z-index: 1001; /* Ensure it's above other elements */
    }

    .save-btn svg {
        fill: #ffffff; /* Original color */
    }

    .save-btn.active svg {
        fill: #FFD700; /* Yellow color when active */
    }

    #save-button:hover {
        background-color: transparent; /* Ensure no grey background on hover */
    }

    wave{
        display: none;
    }
    
.waveform-container {
    display: none;
}

.waveform {
    display: none;
    
   
}



#progress-wave {
    display: none;
}
/* .timeline {
    position: relative;
    bottom: 0;
    width: 100%;
    background-color: rgb(205, 122, 21);
    padding: 0;
    margin: 0;
    height: 50px; /* Adjust timeline height */
    /* z-index: 1000;
    top: 0px; */ 
/* } */

/* .timeline .tick2 {
    display: inline-block;
    width: calc(100% / var(--duration)); 
    height: 2px;
    background-color: #ffffff;
    cursor: pointer;
    margin: 0;
    padding: 0;
    position: absolute;
    bottom: 0;
} */
.timeline {
    position: relative;
    width: 100%;
    /* background-color: rgb(205, 122, 21);  */
    background-color: transparent; 
    height: 50px; /* Height of the timeline */
    z-index: 1000; /* Ensure it is above other elements */
    padding: 0;
    margin: 0;
    --duration: 60;
    bottom: 0px;
    top: 11px;
    margin-top: 480px;
}

.timeline .tick2 {
    display: inline-block;
    width: calc(100% / var(--duration)); 
    height: 2px;
    background-color: #ffffff;
    cursor: pointer;
    position: relative;
    z-index: 7;
    margin: 0;  /* Removes margin */
    padding: 0; /* Removes padding */
    bottom: 10px;
    right: -02px;
}

.bottom-right-sidebar {
    position: fixed;
    bottom: 260px; /* Adjust to align with the video or canvas bottom */
    right: 0; /* Move sidebar to the right side */
    height: auto; /* Consistent with .tools height */
    width: 45px; /* Sidebar width similar to .tools */
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: transparent;
    z-index: 1000; /* Same z-index as .tools */
    padding-top: 12px;
    border-radius: 25px;
    padding-right: 15px; /* Adjust for padding on the right side */
    margin-right: 52px; /* Adjust for margin on the right side */
}



.timeline .tick {
   display: none;
}

#undo, #redo{
    display: none;
}


.tools svg, /* Exclude SVGs or icons from being hidden */
.tools button {
    font-size: initial;
}

.view-icon {
   display:none;
}
#audio-visualizer-button{
display: block;

}

.tools button img {
    width: 27px;
    height: 27px;
    opacity: 1;
}
.tools button svg {
    width: 27px;
    height: 27px;
    opacity: 1;
}

.tools button {
    border-radius: 30px;
    padding: 2px;
    
    
}
#video-btn-id {
    display: flex;
    justify-content: space-between; /* Distribute space between buttons */
    align-items: center; /* Center buttons vertically */
    width: 100%; /* Full width of the container */
    padding: 0 10px; /* Optional padding for spacing */
}


.bottom-right-sidebar button {
    margin: 2px;
    padding: 0.5px;
    width: 30px; /* Match the width with .tools buttons */
    margin-left: 118px;
    text-align: center;
    background-color: rgba(52, 58, 64, 0.9);
    border-radius: 30px; /* Rounded corners similar to .tools buttons */
    padding: 4px;
    transition: background-color 0.3s; /* Smooth background transition */
}

.bottom-right-sidebar button:hover {
    background-color: #485056; /* Keep this as the hover state */
}

.bottom-right-sidebar button img {
    width: 20px; /* Match the icon size with .tools */
    height: 20px;
    opacity: 1; /* Keep full opacity */
}
.bottom-right-sidebar button svg {
    width: 20px; /* Match SVG icon size with .tools */
    height: 20px;
    opacity: 1; /* Keep full opacity */
}
.header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%; /* Full width of the viewport */
    height: 80px; /* Height of the header */
    background-color: rgba(253, 249, 249, 0.3); /* More white, same transparency */

    display: flex;
    justify-content: space-around; /* Space out items with gaps */
    align-items: center; /* Center icons vertically */
    z-index: 1001; /* Ensure it stays above other elements */
    border-radius: 0; /* Remove rounded corners */
}

#library-btn svg{

    width: 25px; /* Icon size */
    height: 25px;

    fill: #abb0c4; /* Icon color */
}

/* Style for the icons inside the header */
.header-icon {
    width: 30px; /* Width for each icon button */
    height: 30px; /* Height for each icon button */
    display: flex;
    justify-content: center;
    align-items: center;
    border-radius: 50%; /* Make the icons round */
    background-color: transparent; /* No background color for buttons */
    cursor: pointer;
    transition: background-color 0.3s; /* Smooth transition for hover */
}

.header-icon img {
    width: 17px; /* Icon size */
    height: 17px;
    fill: #abb0c4; /* Icon color */
}

.header-icon svg {
    width: 17px; /* SVG icon size */
    height: 17px;
    fill: #abb0c4; /* SVG icon color */
}

.header-icon:hover {
    background-color: rgba(0, 0, 0, 0.2); /* Darker background on hover */
}

/* Optional: Active state for icons */
.header-icon:active {
    background-color: rgba(52, 58, 64, 0.9); /* Further darken background on click */
}

/* Specific styles for the save button icon to ensure color is set */
#save-button-mobile svg path {
    fill: #abb0c4; /* Specific color for save button icon */
}

/* Specific styles for the close button icon to ensure color is set */
#close-header-mobile svg path {
    fill: #abb0c4; /* Specific color for close button icon */
}

#savemob-icon {
    width: 20px; /* Set to 20px for a larger icon */
    height: 20px; /* Set to 20px for a larger icon */
    fill: #abb0c4; /* Ensure the icon color remains the same */
}

#frame-container{
    display: none;
}

#upload-svg{
    display: none;
}
#save-button-mobile{
    /* position: absolute;
    top: 10px; 
    right: -90px;  */
    background: none;
    border: none;
    cursor: pointer;
}

#save-button-mobile svg {
    fill: #ffffff; 
}

#save-button-mobile.active svg {
    fill: #FFD700; 
}

#save-button-mobile:hover {
    background-color: transparent; 
}


#screen-recording2{
    width: 20px; 
    height: 20px;
}

}
