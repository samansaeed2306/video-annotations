 /* Adjust for smaller screens */
 body {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    height: 100vh;
    margin: 0;
    overflow: hidden; /* Prevent scrollbars */
}

.tools {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 60px; /* Sidebar width */
    display: flex;
    flex-direction: column;
    align-items: center;
    background-color: #17181a;
    z-index: 1000;
    padding-top: 10px;
}

.tools button {
    margin: 0;
    padding: 5px;
    width: 100%;
    text-align: center;
}

.tools button img {
    width: 20px; /* Reduced icon size */
    height: 20px; /* Reduced icon size */
}

.timeline {
    position: absolute;
    bottom: 0;
    width: calc(100% - 60px); /* Adjust width to account for the sidebar */
    height: 60px;
    background-color: #343a40;
    z-index: 1000;
    left: 60px; /* Move timeline to the right of the sidebar */
    display: none;
}

.buttons-container {
    position: absolute;
    bottom: 60px; /* Ensure it does not overlap with the timeline */
    width: calc(100% - 60px);
    left: 60px;
    display: none;
}

#video-container {
    position: absolute;
    top: 0;
    left: 60px; /* Move video container to the right of the sidebar */
    width: calc(100% - 60px); /* Adjust width to account for the sidebar */
    height: calc(100vh - 60px); /* Adjust height considering the timeline */
    margin: 0;
    padding: 0;
}

video, #canvas {
    width: 100%;
    height: 100%;
    margin: 0;
    padding: 0;
}

.annotations {
    display: none;
}